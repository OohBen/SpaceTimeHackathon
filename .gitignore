# ── Secrets ────────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── Claude Code — generated files (run make-claude-md.sh after cloning) ────
# These are built locally from skills/*.md and AGENTS.md.
# Never commit generated harness artifacts — CRLF/LF differences between
# Windows and Mac teammates cause conflicts even on identical source.
CLAUDE.md
.claude/
AIDER.md
CODEX.md
ANTIGRAVITY.md
GEMINI.md

# ── Other harness directories ───────────────────────────────────────────────
# Each harness generates its own config directory. Keep those local.
# If you use a harness not listed here, add its directory to this section.
.cursor/
.aider*
.continue/
.copilot/
.windsurf/
.codex/
.codex-tmp/
.gemini/

# ── OS ──────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Language / build output ─────────────────────────────────────────────────
# (Extend this section for your project's language)

# Node
node_modules/
dist/
build/
.spacetimedb-local-data/
.next/
.nuxt/
artifacts/
*.log
npm-debug.log*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Go
*.test

# Rust
target/

# Java / JVM
*.class
*.jar
.gradle/

# Ruby
.bundle/

# ── Validation scripts (generated by skill-validate, deleted before PR) ─────
success_check.sh
success_check.py
success_check.ps1

# ── Editors ──────────────────────────────────────────────────────────────────
.idea/
*.swp
*.swo
mnt/
.vercel
